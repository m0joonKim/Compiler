int main(int i, int j) {
  int x = i;
  int y = j;
  int *p = &x;
  int *q;
  int base = 2;
  int acc = 1;

  if (i > 5) {
    q = &y;
  } else {
    q = p;
  }

  if (p == q) {
    base = 3;
  }

  i = 0;
  while (*q > i) {
    acc = acc * base;
    i = i + 1;
  }

  return acc;
}
